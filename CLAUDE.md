# CLAUDE.md - Project Guidance

This project uses semantic versioning. See [VERSIONING.md](./VERSIONING.md) for details.

## Key Points
- Version is stored in `VERSION` file
- Update version in both `VERSION` and `package.json` for any changes
- CI enforces version bump on push to main
